{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "wasm:sync": "node scripts/sync-wasm.mjs",
    "dev": "npm run wasm:sync && vite",
    "build": "npm run wasm:sync && vite build --base=/nn/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "svelte": "^5.55.4",
    "vite": "^8.0.10"
  }
}
